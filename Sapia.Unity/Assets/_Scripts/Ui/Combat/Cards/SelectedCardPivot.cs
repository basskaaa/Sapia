using UnityEngine;

namespace Assets._Scripts.Ui.Combat.Cards
{
    public class SelectedCardPivot : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
